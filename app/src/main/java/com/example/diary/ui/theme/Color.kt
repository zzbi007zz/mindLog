package com.example.diary.ui.theme

import androidx.compose.ui.graphics.Color

// Diary & Journal palette: warm paper + muted ink + a single warm accent.
val PaperLight = Color(0xFFFAF7F2)      // background — warm cream paper
val PanelLight = Color(0xFFF0EBE1)      // surface / cards — slightly deeper paper
val InkLight = Color(0xFF2A2320)        // primary text — warm near-black
val MutedLight = Color(0xFF6B6258)      // secondary text — warm gray
val Hairline = Color(0xFFE2DACB)        // dividers / outlines
val Accent = Color(0xFFB8232C)          // primary action — warm wine red
val AccentContainer = Color(0xFFF4E1E0) // soft accent wash (selected / containers)
val Taupe = Color(0xFF7A7266)           // secondary action — warm taupe